name: Build Extension

on:
    push:
      branches:
        - main
      paths-ignore: # dont run when changes made to these folders
        - '.vscode/**'

jobs:
    build:
        name: Build Extension
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4
              with:
                submodules: recursive

            # - name: install gitversion tool
            #   uses: gittools/actions/gitversion/setup@v0.10.2
            #   with:
            #         versionSpec: '5.x'

            # - name: execute gitversion
            #   id: gitversion # step id used as reference for output values
            #   uses: gittools/actions/gitversion/execute@v0.10.2

            # - name: print gitversion
            #   run: |
            #     echo "Major: ${{ steps.gitversion.outputs.major }}"
            #     echo "Minor: ${{ steps.gitversion.outputs.minor }}"
            #     echo "Patch: ${{ steps.gitversion.outputs.patch }}"
            #     echo "MajorMinorPatch: ${{ steps.gitversion.outputs.majorMinorPatch }}"
            #     echo "SemVer: ${{ steps.gitversion.outputs.semVer }}"

            - name: Install Node
              uses: actions/setup-node@v3
              with:
                node-version: '24'

            - name: Install Packages
              working-directory: Bitmagic.VscExtension
              run: npm install

            - name: Install TypeScript
              working-directory: Bitmagic.VscExtension
              run: npm install -g typescript

            - name: Install VSCE
              working-directory: Bitmagic.VscExtension
              run: npm install -g @vscode/vsce

            # - name: Install Win-Node-Env
            #   working-directory: Bitmagic.VscExtension
            #   run: npm install -g win-node-env

            - name: Compile Extension
              working-directory: Bitmagic.VscExtension
              run: node ./esbuild.js

            - name: Compile and Create VSIX
              working-directory: Bitmagic.VscExtension
              run: vsce package

            - name: Upload VSIX
              uses: actions/upload-artifact@v4
              with:
                name: vsix
                path: Bitmagic.VscExtension/*.vsix
                retention-days: 5

    publish-vscode:
        name: Publish to VSCode Marketplace
        needs: build
        runs-on: ubuntu-latest
        steps:
            - name: Download VSIX
              uses: actions/download-artifact@v4
              with:
                name: vsix
                path: Bitmagic.VscExtension

            - name: Install Node
              uses: actions/setup-node@v3
              with:
                node-version: '24'

            - name: Install VSCE
              run: npm install -g @vscode/vsce

            - name: Publish
              working-directory: Bitmagic.VscExtension
              run: vsce publish -p ${{ secrets.VSCODEDEPLOY }} --packagePath *.vsix

    publish-vscodium:
        name: Publish to Open VSX (VSCodium)
        needs: build
        runs-on: ubuntu-latest
        steps:
            - name: Download VSIX
              uses: actions/download-artifact@v4
              with:
                name: vsix
                path: Bitmagic.VscExtension

            - name: Install Node
              uses: actions/setup-node@v3
              with:
                node-version: '24'

            - name: Install OVSX
              run: npm install -g ovsx

            - name: Publish VSCodium
              working-directory: Bitmagic.VscExtension
              run: npx ovsx publish -p ${{ secrets.VSCODIUM }} --packagePath *.vsix
